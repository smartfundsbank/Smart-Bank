services:
- type: web
  name: smart-bank-backend
  env: python
  buildCommand: pip install -r smartbank-backend/requirements.txt
  startCommand: uvicorn smartbank-backend.main:app --host 0.0.0.0 --port $PORT
  plan: free
