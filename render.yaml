services:
  - type: web
    name: smart-bank-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd smartbank-backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
